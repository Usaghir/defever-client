version: "3"
services:
  database:
    image: postgres
    container_name: defever_db
    env_file:
      - .env
    ports:
      - "5430:5432"
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data:
